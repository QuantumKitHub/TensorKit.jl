name: CompatCheck

on:
  push:
    branches:
      - 'main'
      - 'release-'

    tags: '*'

    paths:
      - 'Project.toml'
      - 'test/Project.toml'

  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]
    paths:
      - 'Project.toml'
      - 'test/Project.toml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Cancel intermediate builds: only if it is a pull request build.
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  setup-matrix:
    runs-on: ubuntu-latest
    outputs:
      groups: ${{ steps.mk.outputs.groups }}
      version: ${{ steps.mk.outputs.version }}
      os: ${{ steps.mk.outputs.os }}
    steps:
      - uses: actions/checkout@v7
      - id: mk
        shell: bash
        run: |
          # Auto-discover test groups from all subdirectory names.
          groups=$(find test -mindepth 1 -maxdepth 1 -type d \
            | xargs -I{} basename {} | sort \
            | jq -R -s -c '[split("\n")[] | select(length > 0)]')

          echo "groups=${groups}" >> "$GITHUB_OUTPUT"
  compatcheck:
    name: "Compat (${{ matrix.group }}, Julia ${{ matrix.julia-version }})"
    needs: setup-matrix
    if: github.event.pull_request.draft != true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        downgrade_mode: ['deps']
        group: ${{ fromJSON(needs.setup-matrix.outputs.groups) }}
        julia-version: ['1.10']
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.julia-version }}
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-downgrade-compat@v2
        with:
          mode: ${{ matrix.downgrade_mode }}
          skip: Random, LinearAlgebra, Test, Combinatorics
          julia_version: ${{ matrix.julia-version }}
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1
        with:
          test_args: '${{ matrix.group }} --fast'
          # This job exists to test the *lowest* compatible versions, so never force the latest
          # ones: that would undo the downgrade above. `auto` resolves to `true` on Dependabot and
          # CompatHelper branches -- exactly the PRs this check matters for -- and then pins each
          # entry to its newest range, which is unsatisfiable here whenever that range requires a
          # newer Julia than this job runs on.
          force_latest_compatible_version: false
        env:
          JULIA_NUM_THREADS: "4"
